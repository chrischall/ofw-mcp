{
  "mcpServers": {
    "ofw": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "OFW_USERNAME": "${OFW_USERNAME}",
        "OFW_PASSWORD": "${OFW_PASSWORD}"
      }
    }
  }
}
