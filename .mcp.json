{
  "mcpServers": {
    "ofw": {
      "command": "tsx",
      "args": ["${CLAUDE_PLUGIN_ROOT}/src/index.ts"],
      "env": {
        "OFW_USERNAME": "${OFW_USERNAME}",
        "OFW_PASSWORD": "${OFW_PASSWORD}"
      }
    }
  }
}
